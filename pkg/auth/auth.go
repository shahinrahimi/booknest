package auth

const (
	Session         string = `session`
	IsAuthenticated string = `is_authenticated`
	IsAdmin         string = `is_admin`
	Username        string = `username`
	UserId          string = `user_id`
)
